// To do: build combat loop

export default function combat(combatXP: number) {
    // Convert XP to level

    // Generate enemy

    function playerTurn() {}

    function enemyTurn() {}

    playerTurn();
}
